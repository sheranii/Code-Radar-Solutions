#include <stdio.h>
int main(){
    int a;
    int b;
    scanf("%d",&a);
    scanf("%d",&b);
    if(a>=18 && (a=1 || a=0)){
        printf("Eligible");
    }
    else{
        printf("Not Eligible");
    }
}